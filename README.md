# SpeedType_Testing
Typing Speed Test web app built using HTML, CSS, and JavaScript. It fetches random quotes via API, tracks typing time, checks accuracy, and displays results instantly. Includes reset functionality and a responsive UI using Bootstrap.
